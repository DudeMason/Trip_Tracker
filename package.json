{
  "name": "trip_tracker",
  "version": "1.0.0",
  "description": "trip tracker",
  "main": "index.js",
  "repository": "https://github.com/DudeMason/Trip_Tracker.git",
  "author": "Mason Eyre <mason.deyre@gmail.com>",
  "license": "MIT",

  "scripts" : {
    "build": "cd client && npm install --only=dev && npm install && npm run build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "heroku-postbuild": "npm run build && npm run deploy && echo 'Client Built'"
  },
}
